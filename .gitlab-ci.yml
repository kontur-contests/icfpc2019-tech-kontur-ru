image: beevee/dotfox:latest

stages:
    - build

check_unchecked:
    stage: build
    script:
        - "dotnet run --project console-runner/console-runner.csproj -- check-unchecked -g geckodriver_docker32"
    when: manual
